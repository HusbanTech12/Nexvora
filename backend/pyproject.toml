[project]
name = "backend"
version = "0.1.0"
description = "Nexvora Backend API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "resend>=0.20.0",
]
